# @whirlpool/format

Shared **byte formatting** for CLI and packages (e.g. `1.2 GiB`, `400 MiB`).

## Main exports

- `formatBytes(n: number): string`

No workspace dependencies. Use anywhere you need consistent human-readable sizes.

## Scripts

`build`, `lint`, `check-types`.
